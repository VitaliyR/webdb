import { wdbEvery } from '../../../helpers/wdb-every';
import { module, test } from 'qunit';

module('Unit | Helper | wdb every');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = wdbEvery(42);
  assert.ok(result);
});
