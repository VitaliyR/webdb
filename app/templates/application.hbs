<div class="site-container">

    <div class="sidebar">
        <ul>
          {{#each table in tables}}
              <li>{{#link-to 'table' table.name}}{{table.name}}{{/link-to}}</li>
          {{/each}}
        </ul>
    </div>

  {{outlet}}

</div>
